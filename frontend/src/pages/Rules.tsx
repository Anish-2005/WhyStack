import React from 'react'

export default function Rules(){
  return (
    <div className="max-w-4xl mx-auto">
      <h1 className="text-2xl font-bold mb-3">Rules</h1>
      <p className="text-sm text-gray-300">Current rule set is simple and lives in the backend engine. It matches combinations of signals (latency, cpu, memory, errors) to causes.</p>
    </div>
  )
}
